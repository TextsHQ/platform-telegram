import { orderBy } from 'lodash'
import { Message, Thread, MessageReaction, MessageSeen, ServerEvent, Participant, MessageAttachmentType, ServerEventType } from '@textshq/platform-sdk'

export function mapMessage(msg: any) {
  return null
}

export function mapThread(thread: any): Thread {
  return null
}
